version: 2

sphinx:
    configuration: docs/src/conf.py

build:
    os: ubuntu-24.04
    tools:
        python: "3.10"
        uv: "latest"
    jobs:
        install:
            - uv sync --group docs
